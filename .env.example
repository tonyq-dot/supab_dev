VITE_SUPABASE_URL=https://your-project.supabase.co
# Use anon key (JWT, starts with eyJ) from Dashboard → Settings → API → Project API keys.
# If you see "Connection Failed", try Legacy anon key instead of Publishable (sb_publishable_).
VITE_SUPABASE_ANON_KEY=your-anon-key
